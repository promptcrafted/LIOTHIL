# Test config — Jinx character LoRA (5-clip subset)
dataset:
  name: jinx
  use_case: character
  description: "Jinx character LoRA — 5-clip test subset from 02_422"

datasets:
  - path: .

video:
  fps: 16
  resolution: 480

controls:
  text:
    anchor_word: Jinx
    required: true
  images:
    reference:
      source: first_frame
