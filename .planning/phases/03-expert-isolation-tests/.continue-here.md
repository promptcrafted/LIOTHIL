---
phase: 03-expert-isolation-tests
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-02-28T04:33:56.419Z
---

<current_state>
Plan 03-01 is COMPLETE (SUMMARY written). Plan 03-02 Task 1 (inference validation script) is in progress — the script runs and generates output, but the output is STILL NOISE. Two fixes were attempted (.eval() on transformers + guidance_scale_2=3.0) based on diff with working test3-inference.py. The quality check numbers changed but Minta confirmed the grids still look like noise visually. The root cause is NOT yet found.
</current_state>

<completed_work>

- Plan 03-01: COMPLETE — both isolation training runs done, sampling bugs fixed, SUMMARY written (commit e72576f)
- Plan 03-02 Task 1 (partial): test4-inference.py created and committed (bd201be), deployed to pod, runs without crash, generates 6 videos + grids + quality report
- Fix attempt 1: Added .eval() to both transformer models (was missing vs working test3 script)
- Fix attempt 2: Added guidance_scale_2=3.0 to all pipeline calls (was missing vs test3)
- Both fixes deployed to pod and re-run — quality check numbers changed but OUTPUT STILL NOISE
</completed_work>

<remaining_work>

- Plan 03-02 Task 1: ROOT CAUSE the inference noise. The .eval() + guidance_scale_2 fixes were NOT sufficient. Minta suspects something fundamental is "unplugged" — possibly related to how the text encoder or model loading works. The T5 text encoder code was verified IDENTICAL between working test3 and broken test4, so it's NOT a T5 loading issue. Need to investigate deeper: possibly the pipeline.__call__() path, how prompt_embeds are consumed internally, or something about the WanPipeline dual-expert dispatch.
- Plan 03-02 Task 2: Present results to Minta for visual review (blocked on Task 1 producing non-noise output)
</remaining_work>

<decisions_made>

- Confirmed T5 text encoder loading is IDENTICAL between test3 (working) and test4 (broken) — same path, same config, same embed_tokens fix, same eval(), same encoding
- Confirmed transformer loading uses same config= and subfolder= parameters in both scripts
- Confirmed VAE loading is identical (float32, same path)
- Confirmed scheduler is identical (FlowMatchEulerDiscrete, shift=5.0)
- Added .eval() to transformers — test3 had it, test4 didn't. Changed quality numbers but didn't fix noise.
- Added guidance_scale_2=3.0 — test3 had it, test4 didn't. Also didn't fix noise.
- Bug found during 03-02 execution: diffusers load_lora_weights() requires transformer. prefix even for load_into_transformer_2=True (low-noise LoRA keys get transformer_2. -> transformer. prefix swap)
</decisions_made>

<blockers>
- CRITICAL: test4-inference.py produces noise for ALL conditions (base, low-LoRA, high-LoRA). Since even base model (no LoRA) produces noise, the issue is in the core inference pipeline, not LoRA loading.
- The working test3-inference.py was validated in Phase 1. The code LOOKS nearly identical but something is different in behavior.
- Minta's intuition: "something is unplugged" — like an earlier Phase 1 bug where wrong text encoder file was loaded
- Pod is still running with all checkpoints available
</blockers>

<context>
The core mystery: test3-inference.py works, test4-inference.py doesn't, and a line-by-line diff shows the T5/transformer/VAE/scheduler code is essentially identical. The remaining differences found and fixed (.eval(), guidance_scale_2) didn't solve it.

Possible investigation paths not yet explored:
1. Run test3-inference.py on the pod RIGHT NOW to confirm it still works (maybe pod state changed?)
2. Diff the actual pipeline.__call__() execution — maybe prompt_embeds shape or dtype is wrong by the time it reaches the model
3. Check if the .cpu()/.to("cuda") round-trip for embeddings introduces a subtle issue
4. Print intermediate tensors during denoising (latent stats at each step) to see where noise enters
5. Check if WanPipeline handles text_encoder=None differently in this diffusers version
6. Compare with how the training sampler (dimljus/training/sampler.py) calls inference — it DOES produce working output during training now (after the partner model fix)

The training sampler works. test3 works. test4 doesn't. The difference must be in some detail we haven't spotted yet.
</context>

<next_action>
Start with: Run test3-inference.py on the pod to confirm it still produces clean output. If test3 still works, do a BINARY diff approach — start from test3, add test4's features one at a time until it breaks. If test3 is ALSO broken now, then the pod environment changed (maybe a package got upgraded).
</next_action>
