---
phase: 03-expert-isolation-tests
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-02-28T03:06:46.152Z
---

<current_state>
Plan 03-01 (Wave 1) training runs are COMPLETE on RunPod — both test4 (low-noise) and test5 (high-noise) finished 5 epochs each with decreasing loss. However, training samples were pure noise due to two bugs that have been fixed and committed but NOT yet validated with a re-run.

Plan 03-02 (Wave 2) has NOT started — it depends on 03-01 having valid samples for Minta to review.
</current_state>

<completed_work>

- Plan 03-01 Task 1: Create isolation YAML configs — DONE (commit `5b4f91f`)
- Plan 03-01 Task 2: Execute training runs — DONE (both completed on pod)
  - test4 low-noise: 5 epochs, checkpoints at `/workspace/outputs/test4-low-only/low_noise/`
  - test5 high-noise: 5 epochs, checkpoints at `/workspace/outputs/test5-high-only/high_noise/`
  - W&B project `dimljus-isolation` has both runs with loss curves
  - Loss EMA clearly decreasing for both runs
  - BUT: samples are noise (two bugs found — see decisions_made)
</completed_work>

<remaining_work>

- Plan 03-01: Re-validate samples with fixes applied — either re-run 1 epoch of training OR run standalone inference on saved checkpoints to prove sampling works
- Plan 03-01: Create SUMMARY.md once samples are validated
- Plan 03-02 Task 1: Run post-training inference and quality checks (script `runpod/test4-inference.py`)
- Plan 03-02 Task 2: CHECKPOINT — Present results to Minta for visual review
</remaining_work>

<decisions_made>

- **Bug 1 — Single expert produces noise**: Wan 2.2 experts are specialists. The low-noise expert only handles low-noise timesteps, high-noise only handles high-noise. Using one alone for full 30-step denoising = pure noise. Fix: always load the partner base model during expert phase sampling, even without a partner LoRA. Committed `387155c`.
- **Bug 2 — diffusers 0.36.0 regression**: WanPipeline in 0.36.0 produces washed-out white output (pixel mean 0.91 vs expected 0.25). Downgraded pod to 0.35.2, pinned `<0.36` in pyproject.toml. Committed `e05f18b`.
- **Pod deployment**: dimljus on pod is NOT a git repo (tarball deploy). Updated `loop.py` was SCP'd directly. For full redeploy, need to re-clone or scp all changed files.
</decisions_made>

<blockers>
- Need to verify sampling fixes produce clean output before creating SUMMARY.md
- Pod dimljus is not a git repo — `setup.sh` git pull fails. Need to either re-clone or scp changed files individually.
</blockers>

<context>
The training pipeline IS working — loss is decreasing, checkpoints are valid. The only issue was preview sample generation during training. Two independent bugs conspired: (1) the code skipped loading the partner expert in isolation mode, and (2) a diffusers version upgrade broke WanPipeline output normalization. Both are now fixed in the codebase and on the pod.

The fastest path forward is to run standalone inference using the saved checkpoints (no need to re-train) to verify the sampling fixes, then proceed to Plan 03-02 (inference validation + Minta review).
</context>

<next_action>
Start with: Run standalone inference on saved test4/test5 checkpoints using the fixed sampling pipeline (diffusers 0.35.2 already on pod, loop.py already SCP'd). This validates both the checkpoints AND the sampling fixes in one shot. Then create 03-01-SUMMARY.md and proceed to 03-02.

Quick validation command on pod:
```bash
# SCP any remaining changed files if needed, then run a quick test:
python3 -c "... dual-expert inference test ..."  # See memory for pattern
```
</next_action>
