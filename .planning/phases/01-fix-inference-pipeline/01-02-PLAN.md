---
phase: 01-fix-inference-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - runpod/test_base_inference.py
autonomous: false
requirements:
  - INFER-01
  - INFER-02
  - INFER-03

must_haves:
  truths:
    - "Base model inference (no LoRA) produces a video with recognizable content matching the text prompt"
    - "LoRA inference produces output visually different from base model output"
    - "Keyframe grids show coherent frames (not noise, not grids, not static)"
    - "Training pipeline sample generation uses the same fixed code path as standalone scripts"
  artifacts:
    - path: "runpod/test_base_inference.py"
      provides: "GPU-validated standalone test script"
  key_links:
    - from: "runpod/test_base_inference.py"
      to: "dimljus/training/wan/inference.py"
      via: "Both use WanTransformer3DModel.from_single_file with config= parameter"
      pattern: 'config="Wan-AI/Wan2.2-T2V-A14B-Diffusers"'
---

<objective>
Validate the inference fix on GPU hardware by running base model and LoRA inference, then visually confirming the output is coherent video.

Purpose: The code fix from Plan 01 is deterministic (adding config= parameters), but the actual proof that inference works requires running on a GPU with the real Wan 2.2 model weights. This validation is the gate for all downstream phases — nothing else can be tested until we see real video output.

Output: Visual confirmation via keyframe grids and MP4 videos that both base model and LoRA inference produce recognizable content.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fix-inference-pipeline/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare and push code to RunPod</name>
  <files>runpod/test_base_inference.py</files>
  <action>
Ensure all changes from Plan 01 are committed and pushed. Then verify the RunPod pod has the latest code:

1. Check git status — all Plan 01 changes should already be committed
2. Push to the remote: `git push origin main`
3. The pod pulls from the repo via `cd /workspace/dimljus && git pull` (Minta runs this)

**Escalation plan if base inference still produces noise after the config= fix:**

Follow the locked escalation order from CONTEXT.md:
1. ~~diff → model loading~~ (this IS the model loading fix)
2. Try `from_pretrained` with full HF repo `"Wan-AI/Wan2.2-T2V-A14B-Diffusers"` — modify test_base_inference.py to use `from_pretrained` instead of `from_single_file` as a quick A/B test
3. If `from_pretrained` also fails → build a minimal stock diffusers reference pipeline (pure diffusers, no dimljus code at all) as ground truth

This escalation should be documented in the test script as comments so Minta can follow along.
  </action>
  <verify>
    <automated>cd C:/Users/minta/Projects/dimljus-kit && git log --oneline -3 && git status --short</automated>
  </verify>
  <done>Latest code with config= fix is pushed to remote and ready for pod deployment</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual validation of inference output on RunPod GPU</name>
  <files>runpod/test_base_inference.py</files>
  <action>
Fixed inference pipeline with explicit model configuration on all from_single_file() calls. The fix prevents diffusers from silently loading Wan 2.1 config instead of Wan 2.2 (diffusers#12329). Keyframe grids are now generated alongside every sample video for quick visual review.

**Minta verifies on the RunPod pod:**

1. Pull latest code:
   ```
   cd /workspace/dimljus && git pull origin main
   ```

2. Run base inference test:
   ```
   cd /workspace && python /workspace/dimljus/runpod/test_base_inference.py
   ```

3. Check the output files:
   - Look at the `.grid.png` keyframe grids first (quickest visual check)
   - If grids show recognizable content -> base inference (INFER-01) is FIXED
   - If grids show noise -> escalate per the plan (try from_pretrained)

4. Check LoRA test output:
   - The test script runs with and without LoRA (if test2/test3 checkpoints exist)
   - Compare base vs LoRA keyframe grids -- any visible difference = LoRA working (INFER-02)

5. For training pipeline integration (INFER-03):
   - Run a 1-epoch training with sampling enabled to verify the same code path works from training
   - OR: confirm that backend.py load_model() uses the same config= fix

**Expected output:**
- .mp4 videos with recognizable content (person walking, scene matching prompt)
- .grid.png keyframe grids showing 5 frames of coherent video
- No noise, no grid artifacts, no static frames
- LoRA output visually different from base output (even slightly -- 3 epochs is minimal)

**If it fails:**
- Report what the output looks like (noise? grids? partial content?)
- Report the exact error messages if any
- Type "escalate" and Claude will implement the next step in the escalation sequence

**Resume signal:** Type "approved" (works), "escalate" (still noise), or describe what you see.
  </action>
  <verify>
    <automated>cd C:/Users/minta/Projects/dimljus-kit && echo "Requires GPU validation on RunPod -- visual inspection of output files"</automated>
  </verify>
  <done>
    - Minta confirms base inference produces coherent video (INFER-01)
    - Minta confirms LoRA output differs from base (INFER-02)
    - Same code path works standalone and from training pipeline (INFER-03)
  </done>
</task>

</tasks>

<verification>
1. Base model output: keyframe grid shows recognizable content matching prompt (not noise)
2. LoRA output: keyframe grid is visually different from base output
3. Both standalone scripts and training pipeline use the same fixed model loading code
4. Output format: .mp4 video + .grid.png keyframe grid for every sample
</verification>

<success_criteria>
- Minta visually confirms base inference produces coherent video (INFER-01)
- Minta visually confirms LoRA inference differs from base (INFER-02)
- Same inference code path works from both standalone scripts and training pipeline (INFER-03)
- If escalation needed, the next step in the sequence is clearly documented
</success_criteria>

<output>
After completion, create `.planning/phases/01-fix-inference-pipeline/01-02-SUMMARY.md`
</output>
